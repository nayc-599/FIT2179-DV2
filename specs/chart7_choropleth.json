{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "title": {
    "text": "Total emigrant departures by state and territory",
    "subtitle": "Sum of all countries of birth, 2004–05 to 2024–25",
    "font": "DM Sans",
    "subtitleFontSize": 12,
    "subtitleColor": "#555"
  },
  "width": 550,
  "height": 420,
  "projection": {
    "type": "conicEqualArea",
    "parallels": [-18, -36],
    "rotate": [-134, 0],
    "center": [0, -27],
    "scale": 750
  },
  "data": {
    "url": "data/australian-states.json",
    "format": {"type": "json", "property": "features"}
  },
  "transform": [
    {
      "lookup": "properties.STATE_NAME",
      "from": {
        "data": {"url": "data/chart7_choropleth.csv"},
        "key": "state_name",
        "fields": ["departures", "state", "longitude", "latitude"]
      }
    }
  ],
  "layer": [
    {
      "mark": {
        "type": "geoshape",
        "stroke": "#ffffff",
        "strokeWidth": 0.8
      },
      "encoding": {
        "color": {
          "field": "departures",
          "type": "quantitative",
          "title": "Total departures",
          "scale": {
            "type": "log",
            "scheme": "blues"
          },
          "legend": {
            "format": ",.0f",
            "titleFontSize": 11,
            "labelFontSize": 10
          }
        },
        "tooltip": [
          {"field": "properties.STATE_NAME", "title": "State"},
          {"field": "departures", "title": "Total departures", "format": ","}
        ]
      }
    },
    {
      "mark": {
        "type": "text",
        "fontSize": 11,
        "fontWeight": 600,
        "fill": "#333",
        "align": "center",
        "baseline": "middle"
      },
      "encoding": {
        "longitude": {"field": "longitude", "type": "quantitative"},
        "latitude": {"field": "latitude", "type": "quantitative"},
        "text": {"field": "state"},
        "tooltip": [
          {"field": "properties.STATE_NAME", "title": "State"},
          {"field": "departures", "title": "Total departures", "format": ","}
        ]
      }
    }
  ],
  "config": {"background": "transparent",
    "view": {"stroke": null},
    "font": "DM Sans"
  }
}